mutation CancelBooking(
    $id: Int!
    $bookingNum: String!
    $subResNum: String!
    $source: Int!
) {
    cancelBooking(
        id: $id
        bookingNum: $bookingNum
        subResNum: $subResNum
        source: $source
    )
}
